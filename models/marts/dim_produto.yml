version: 2

models:
    - name: dim_produto
      description: tabela com informacoes sobre os produtos comercializados na operacao
      config:
        materialized: table
      columns:
        - name: id_produto
          tests:
            - not_null
 